TASK-79 remediation: added deterministic BehaviorSpec tests for Daily and Hourly Reminder jobs. Each mocks a 1,000-item first batch, then requires the second port query to use the prior batch's last schedule ID as cursor. No production/config changes. Focused ReminderModuleTest and `../gradlew :dymit-backend-api:build` passed; `git diff --check` passed.
