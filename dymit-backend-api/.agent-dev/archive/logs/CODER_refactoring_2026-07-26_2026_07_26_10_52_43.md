# TASK-73 코더 결과

study_schedule을 최상위 모듈로 이동하고 `adapter/in·out`, `application/port/in·out` 구조를 적용했다. 그룹·파일 outbound 포트와 조회·멤버 이벤트·일정 이벤트 inbound 계약을 추가해 task·reminder·board의 직접 엔티티/저장소 참조를 제거했다. REST 경로와 Mongo 컬렉션은 유지했으며 `@TypeAlias`로 기존 `_class` 값을 보존했다. 전체 빌드와 패키지 경계 검사를 통과했다.
