Completed TASK-79 production modularization. Moved Task domain, web APIs/adapters, use cases, persistence ports/adapters, events, and DTOs under `task/` with exact escaped `in`/`out` packages. All 20 use cases now expose `execute(Command/Input)` using port-owned DTOs. Removed the obsolete compatibility service and legacy Task production packages. Replaced Task push-domain coupling with common push event DTOs. `compileKotlin` and `assemble` passed. Tests were not edited.
